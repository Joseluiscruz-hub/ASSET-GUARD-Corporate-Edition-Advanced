import { describe, it, expect } from 'vitest';

describe('App Init', () => {
  it('should run tests successfully', () => {
    // Prueba base de inicialización para que la CI pase exitosamente.
    expect(true).toBe(true);
  });
});
